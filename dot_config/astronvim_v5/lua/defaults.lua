_G.myvim = {
  plugins = {
    is_development_machine = false,
    is_corporate_machine = false,

    lsp = {
      servers_from_system = {},
      ensure_installed = {},
      exclude_from_ensure_installed = {},
    },
  },
}
