_G.myvim = {
  root_markers = { ".git", ".root", ".project", "_darcs", ".hg", ".bzr", ".svn", "Makefile", "package.json" },

  colorscheme_enable_italic = true,
  colorscheme_enable_italic_comment = true,

  plugins = {
    tabnine = { active = true },
  },
}
